# Suppress spurious http-proxy config warning from older npm versions.
# This project does not use an HTTP proxy; setting it explicitly to empty
# prevents the "Unknown env config http-proxy" noise on runner environments
# that have NPM_CONFIG_HTTP_PROXY set in the process environment.
http-proxy=
https-proxy=
