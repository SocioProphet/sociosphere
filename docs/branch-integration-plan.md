# Branch Integration Evaluation and Execution

This document is generated by `tools/branch_integration_audit.py`.

- Base branch: `work`
- Branch refs discovered: `1`
- Remotes configured: `0`
- Branch catalog: `workbench/backlog/branch-catalog.tsv`
- Wave plan: `governance/merge-wave-plan.md`

## Execution status

No git remotes are configured in this checkout, so only local refs can be evaluated.
To move forward, add the authoritative remote and rerun:

```bash
git remote add origin <url>
git fetch origin --prune --tags
python3 tools/branch_integration_audit.py --base-branch work
```

## Machine summary

```json
{
  "date_utc": "2026-04-06T04:52:00+00:00",
  "base_branch": "work",
  "branch_count": 1,
  "remote_count": 0,
  "catalog_path": "workbench/backlog/branch-catalog.tsv",
  "wave_plan_path": "governance/merge-wave-plan.md",
  "remotes": []
}
```
